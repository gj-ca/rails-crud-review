<%= link_to "New", events_new_path %>
<% @events.each do |event| %>
  <h1><%= event.name %></h1>
  <h2>Date: <%= event.date %></h2>
  <h2>Time: <%= event.time %></h2>
  <h5>Organized by <%= event.organizer.email %></h5>
  <%= link_to "Show", event %>
<% end %>